fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
openai>=1.30,<2.0
httpx>=0.27,<1.0
pypdf>=4,<7
ddgs>=9,<10
python-multipart>=0.0.9,<1.0
cryptography>=42
# Offline, CPU-only text-to-speech (Kokoro-82M). Pulls in torch + numpy.
# Optional: the app runs fine without it — the speaker button just stays hidden.
kokoro>=0.7,<1.0
# Bundles a prebuilt espeak-ng library (no system install) so Kokoro's G2P can
# pronounce out-of-vocabulary words — proper names, foreign words — instead of
# silently dropping them. Without it the speaker button still works but skips
# unknown names.
espeakng-loader>=0.2,<1.0
